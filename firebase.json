{
  "database": {
    "rules": "database.rules.json"
  },
  "functions": {
    "predeploy": [
      "npm --prefix $RESOURCE_DIR run lint"
    ]
  },
  "hosting": {
    "public": "build/default",
    "ignore": [
      "firebase.json"
    ]
  },
  "storage": {
    "rules": "storage.rules"
  }
}
